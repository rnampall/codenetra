## What this PR does

A short summary of the change. Linked issue: #

## Why

What's the problem this solves, or what's the value to users?

## How it's tested

- [ ] Existing offline verifier scripts still pass
- [ ] Smoke-tested locally against `--group <something>`
- [ ] (If touching the web UI) opened the local server and exercised the flow
- [ ] (If adding a rule) confirmed it passes on a synthetic compliant repo
      and fails on a non-compliant one

## Checklist

- [ ] Updated `CHANGELOG.md` under "Unreleased"
- [ ] Updated docs / README if behaviour changed
- [ ] No new runtime dependencies (or discussed in linked issue)
